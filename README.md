# OutreachPilot

Mobile-first prospect discovery and outreach CRM.

## Run locally

```bash
npm install
npm run build
npm start
```

## Environment

Copy `.env.example` to `.env` and configure the server-side Gemini key if required by the deployment.

Do not commit `.env` or API keys.
